<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thirteenash.mapper.UserMapper">

    <resultMap id="UserMap" type="com.thirteenash.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="realName" column="real_name"/>
        <result property="studentId" column="student_id"/>
        <result property="age" column="age"/>
        <result property="gender" column="gender"/>
        <result property="avatar" column="avatar"/>
        <result property="role" column="role"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="status" column="status"/>
    </resultMap>

    <select id="selectById" resultMap="UserMap">
        SELECT * FROM t_user WHERE id = #{id}
    </select>

    <select id="findByUsername" resultMap="UserMap">
        SELECT * FROM t_user WHERE username = #{username} AND status = 'NORMAL'
    </select>

    <insert id="insert" parameterType="com.thirteenash.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user (
            username,
            password,
            email,
            phone,
            real_name,
            student_id,
            age,
            gender,
            avatar,
            role,
            create_time,
            update_time,
            status
        ) VALUES (
            #{username},
            #{password},
            #{email},
            #{phone},
            #{realName},
            #{studentId},
            #{age},
            #{gender},
            #{avatar},
            #{role},
            #{createTime},
            #{updateTime},
            #{status}
        )
    </insert>

    <update id="updateById" parameterType="com.thirteenash.entity.User">
        UPDATE t_user
        SET
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="studentId != null">student_id = #{studentId},</if>
            <if test="age != null">age = #{age},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="role != null">role = #{role},</if>
            <if test="status != null">status = #{status},</if>
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM t_user WHERE id = #{id}
    </delete>

</mapper>